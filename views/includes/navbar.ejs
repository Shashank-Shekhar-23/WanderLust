<nav class="navbar bg-body-light sticky-top border-bottom">
    <div class="container-fluid d-flex align-items-center justify-content-between">
        <!-- Brand/Logo -->
        <div class="d-flex align-items-center justify-content-between">
            <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
            <a class="nav-link nav-explore d-none d-lg-block me-1 col-text" href="/listings">Explore</a>
        </div>

        <div class="d-flex align-items-center gap-2 gap-md-3">

            <form action="/listings" class="d-flex align-items-center search-form" role="search" method="GET">
                <input type="text" name="search" class="form-control search-inp me-1 me-md-2"
                    placeholder="Search destination" value="<%= typeof search !== 'undefined' ? search : '' %>">
                <button class="btn btn-search align-items-center justify-content-center d-xsm-none d-sm-flex" type="submit">
                    <i class="fa-solid fa-magnifying-glass text-white"></i>
                    <span class="btn-search-text ms-1 text-white">Search</span>
                </button>
            </form>

        </div>
        <div class="d-flex align-items-center gap-2 gap-md-3">

            <!-- Become Host (hidden in navbar on screen < 768px, shown next to tax toggle instead) -->
            <a class="nav-link nav-become-host d-none d-md-block col-text" href="/listings/new">Become Host</a>

            <div class="auth-links d-flex align-items-center gap-2 gap-md-3">
                <% if (!currUser) { %>
                    <a class="nav-link" href="/signup"><b>Sign Up</b></a>
                    <a class="nav-link" href="/login"><b>Log in</b></a>
                <% } else { %>
                        <a class="nav-link" href="/logout"><b>log out</b></a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>
